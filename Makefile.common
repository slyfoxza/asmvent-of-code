.PHONY: all clean

PROGRAMS := $(subst .$(ARCHITECTURE).s,,$(wildcard ????-??-?.$(ARCHITECTURE).s))

all: $(PROGRAMS)

clean:
	$(RM) *.o $(PROGRAMS)

%.o: %.$(ARCHITECTURE).s
	$(AS) $(ASFLAGS) -o $@ $^
