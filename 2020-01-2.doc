This is pretty much the same deal as 2020-01-1, except now we multiply by n
again to get O(n^3), with yet another nested loop:

    for(i = 0; i < n - 2; ++i) {
      for(j = i + 1; j < n - 1; ++j) {
        for(k = j + 1; k < n; ++k) {
          ...
